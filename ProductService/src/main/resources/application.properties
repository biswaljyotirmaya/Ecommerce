spring.application.name=ProductService

#Datbase properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql:///ntspbms618db
spring.datasource.username=root
spring.datasource.password=safari

#Hibernate Propertties
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

product:
	module:
		message:
		 save-success:Product is registered with id
		 save-failure= Product not registered
		 update-success= Product is updated successdully
         update-failure= Product not found for updation
         delete-success= Product deleted successfully
         delete-failure= Product not find for deletion
